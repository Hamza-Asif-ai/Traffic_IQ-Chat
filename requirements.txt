setuptools<81  # deep-sort-realtime 1.3.2 still imports pkg_resources internally,
               # which newer setuptools (v81+) removed -- pin this or fresh installs crash.
ultralytics==8.2.0
deep-sort-realtime==1.3.2
opencv-python-headless==4.9.0.80
streamlit==1.35.0
numpy==1.26.4
pandas==2.2.2
plotly==5.22.0
matplotlib==3.8.4
Pillow==10.3.0
groq==0.11.0
httpx<0.28  # groq==0.11.0 passes proxies= to httpx.Client, which httpx 0.28+ removed -- pin or every chatbot call crashes
python-dotenv==1.0.1
ipykernel==6.29.4  # needed so VS Code/Jupyter can register this venv as a notebook kernel
